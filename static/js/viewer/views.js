/**
 * Views for the story viewer Backbone application
 */
Namespace('storybase.viewer');

storybase.viewer.views.StoryHeaderView = Backbone.View.extend({
  render: function() {
    return this;
  }
});

storybase.viewer.views.StoryNavigationView = Backbone.View.extend({
  render: function() {
    return this;
  }
});
